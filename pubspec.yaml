name: loan_collection_android
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.1.0
  http: ^0.13.5
  google_fonts: ^4.0.3
  device_info_plus: ^8.2.0
  sprintf: ^7.0.0
  esc_pos_utils: ^1.1.0
  bluetooth_thermal_printer: ^0.0.6
  intl: ^0.18.1
  flutter_svg: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - images/
